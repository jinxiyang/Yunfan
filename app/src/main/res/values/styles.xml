<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--日/夜间主题颜色-->
        <item name="whiteContentBackground">@color/whiteContentBackground</item>
        <item name="grayContentBackground">@color/grayContentBackground</item>
        <item name="blackTextColor">@color/blackTextColor</item>
        <item name="grayTextColor">@color/grayTextColor</item>
        <item name="whiteTextColor">@color/whiteTextColor</item>
        <item name="grayLineColor">@color/grayLineColor</item>


    </style>

    <style name="TransparentTheme" parent="AppTheme"></style>


    <!--首页新闻频道的字体大小-->
    <style name="NewsChannelAppearance">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="BlackStatusTextColorTheme" parent="AppTheme">
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
